/* CHECK CONSTRAINT */

-- DE FORMA SIMPLES: NOS AJUDA A IMPOR RESTRIÇÕES PARA VALORES

-- SINTAXE BASICA: 

CREATE TABLE Tabela
(
	CampoUm VARCHAR(100) NOT NULL,
	CampoDois DATETIME DEFAULT CURRENT_TIMESTAMP,
	CampoTres VARCHAR(100) NOT NULL,
	CampoQuatro INT CHECK (PEG >= 0 AND PEG <=18)
);
